/* 25/09/2023 */
#include <iostream>
#include <windows.h>

using namespace std;
/*   
    / ; () > [] ? : $ @ & %  # "" = _ !
*/
/*
    Una lista lineal en la que cada elemento tiene un único sucesor.
    4 tipos de operaciones: inserción, supresión, recuperación y recorrido.
    Los elementos de una lista lineal contigua se almacenenan en la memoria en
    posiciones  sucesivas o adyacentes y se procesan fomo un array unidimensional
    La inserción o borrado requiere un desplazamiento de los elementos.
    Tipo de listas: 
    - listas simplemente enlazadas (contiene un único enlace que conecta al nodo siguiente)
    - listas doblemente enlazadas (cada nodo contiene dos enlaces, con el siguiente y el anterior)
    - lista simple circular (la cola se enlaza con la cabeza)
    - lista doblemente enlazada circular (la cola se enlaza con la cabeza y viceversa)
    )
    
*/
int main(){
    // Función que permite mostrar los caracteres especiales en la terminal
    SetConsoleOutputCP(CP_UTF8);
    return 0;
}
